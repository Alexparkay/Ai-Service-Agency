import Reveal from '@/components/Reveal';

export default function Problem() {
  return (
    <section className="section">
      <Reveal>
        <h2 className="headline text-3xl md:text-5xl">The Problem</h2>
      </Reveal>
      <p className="subheadline mt-4 max-w-3xl">
        Agencies optimized for labor-based delivery struggle as clients expect automation-first
        outcomes. Without an AI-native core, margins compress and churn increases.
      </p>
    </section>
  );
}


